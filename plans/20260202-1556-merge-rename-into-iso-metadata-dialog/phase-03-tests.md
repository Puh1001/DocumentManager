## Phase 03 - Build & tests

### Tasks

- Run typecheck/build to ensure compile success:
  - `npm run build` (monorepo)
- Run relevant tests (if configured):
  - `npm run test` or targeted web/api tests as repo defines
